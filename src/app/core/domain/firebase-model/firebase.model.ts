export interface FirebaseModel {
    apiKey: string;
    authDomain: string;
    projectId: string;
    storageBucket: string;
    messagingSenderId: string;
    appId: string;
}

export interface UserImpl {
    accessToken: string
}
